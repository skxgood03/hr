# 人事公司工资管理系统 - 二次开发方案

## 项目概述

### 项目背景
基于现有人力资源管理系统进行二次开发，专注于人事公司工资管理系统的核心功能。保留现有的部门管理、个人信息管理、岗位管理模块，重新设计和实现工资管理相关功能。

### 项目目标
- 构建专业的人事公司工资管理系统
- 提供完整的工资核算、发放、统计功能
- 支持多种薪资结构和计算规则
- 提供直观的前端界面和良好的用户体验

## 系统架构分析

### 现有系统架构
- **技术栈**: Django 5.1.7 + SQLite + HTML/CSS/JavaScript
- **架构模式**: 前后端不分离的传统Web应用
- **数据库**: SQLite（开发环境）
- **模板引擎**: Django Templates
- **静态文件**: CSS/JavaScript

### 保留模块分析

#### 1. 部门管理 (department)
- **模型**: Department
- **字段**: departmentName, description
- **评价**: 结构简单清晰，适合基础部门管理

#### 2. 个人信息管理 (personal)
- **模型**: Personal
- **字段**: 包含员工基本信息（姓名、性别、生日、电话、邮箱、身份证、学历等）
- **关联**: 与Station模型外键关联
- **评价**: 字段完整，满足员工信息管理需求

#### 3. 岗位管理 (station)
- **模型**: Station
- **字段**: stationName, description
- **关联**: 与Department模型外键关联
- **评价**: 建立了部门-岗位-员工的层级关系

### 需要删除的模块

#### 1. 薪资管理 (salary)
- **问题**: 模型设计过于复杂，存在冗余结构
- **删除原因**: 重新设计更适合人事公司的薪资管理模型

#### 2. 考勤管理 (attendance)
- **问题**: 功能过于复杂，不符合人事公司核心需求
- **删除原因**: 人事公司主要关注工资管理，考勤可简化或外包

#### 3. 奖惩管理 (rewards)
- **问题**: 模型设计简单，功能不完整
- **删除原因**: 将奖惩功能整合到新的工资管理模块中

## 需求分析

### 功能需求

#### 1. 工资管理核心功能

##### 1.1 薪资标准管理
- 薪资等级定义
- 基本工资标准设置
- 岗位工资标准配置
- 薪资调整历史记录

##### 1.2 工资计算
- 基本工资计算
- 绩效工资计算
- 津贴补贴计算
- 奖金计算
- 扣款计算（社保、公积金、个税等）
- 实发工资计算

##### 1.3 工资发放管理
- 月度工资核算
- 工资发放审批
- 工资条生成
- 银行代发文件生成
- 发放状态跟踪

##### 1.4 工资统计分析
- 部门工资统计
- 岗位工资分析
- 工资成本分析
- 工资趋势分析
- 个人工资历史

#### 2. 辅助功能

##### 2.1 系统管理
- 用户权限管理
- 系统参数配置
- 数据备份恢复
- 操作日志记录

##### 2.2 报表功能
- 工资汇总表
- 部门工资明细表
- 个人工资条
- 工资成本分析报表
- 税务申报表

### 非功能需求

#### 1. 性能需求
- 支持1000+员工数据处理
- 工资计算响应时间<5秒
- 报表生成时间<10秒

#### 2. 安全需求
- 用户身份认证
- 数据访问权限控制
- 敏感数据加密存储
- 操作审计日志

#### 3. 可用性需求
- 界面友好，操作简单
- 支持主流浏览器
- 响应式设计，支持移动端查看

## 技术方案

### 1. 数据库设计

#### 1.1 核心数据表

```sql
-- 薪资等级表
CREATE TABLE t_salary_grade (
    id INTEGER PRIMARY KEY,
    grade_name VARCHAR(50) NOT NULL,
    min_salary DECIMAL(10,2) NOT NULL,
    max_salary DECIMAL(10,2) NOT NULL,
    description TEXT,
    is_active BOOLEAN DEFAULT TRUE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 员工薪资配置表
CREATE TABLE t_employee_salary_config (
    id INTEGER PRIMARY KEY,
    employee_id INTEGER NOT NULL,
    salary_grade_id INTEGER NOT NULL,
    basic_salary DECIMAL(10,2) NOT NULL,
    performance_salary DECIMAL(10,2) DEFAULT 0,
    allowance DECIMAL(10,2) DEFAULT 0,
    effective_date DATE NOT NULL,
    end_date DATE,
    is_active BOOLEAN DEFAULT TRUE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (employee_id) REFERENCES t_personal(id),
    FOREIGN KEY (salary_grade_id) REFERENCES t_salary_grade(id)
);

-- 月度工资记录表
CREATE TABLE t_monthly_salary (
    id INTEGER PRIMARY KEY,
    employee_id INTEGER NOT NULL,
    salary_month DATE NOT NULL,
    basic_salary DECIMAL(10,2) NOT NULL,
    performance_salary DECIMAL(10,2) DEFAULT 0,
    allowance DECIMAL(10,2) DEFAULT 0,
    bonus DECIMAL(10,2) DEFAULT 0,
    overtime_pay DECIMAL(10,2) DEFAULT 0,
    gross_salary DECIMAL(10,2) NOT NULL,
    social_insurance DECIMAL(10,2) DEFAULT 0,
    housing_fund DECIMAL(10,2) DEFAULT 0,
    income_tax DECIMAL(10,2) DEFAULT 0,
    other_deduction DECIMAL(10,2) DEFAULT 0,
    total_deduction DECIMAL(10,2) NOT NULL,
    net_salary DECIMAL(10,2) NOT NULL,
    status VARCHAR(20) DEFAULT 'DRAFT',
    pay_date DATE,
    bank_account VARCHAR(50),
    remarks TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (employee_id) REFERENCES t_personal(id),
    UNIQUE(employee_id, salary_month)
);

-- 工资调整记录表
CREATE TABLE t_salary_adjustment (
    id INTEGER PRIMARY KEY,
    employee_id INTEGER NOT NULL,
    adjustment_type VARCHAR(20) NOT NULL,
    old_salary DECIMAL(10,2) NOT NULL,
    new_salary DECIMAL(10,2) NOT NULL,
    adjustment_amount DECIMAL(10,2) NOT NULL,
    effective_date DATE NOT NULL,
    reason TEXT,
    approver_id INTEGER,
    approval_date DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (employee_id) REFERENCES t_personal(id)
);
```

### 2. 后端架构设计

#### 2.1 Django应用结构
```
salary_management/
├── models.py          # 数据模型
├── views.py           # 视图函数
├── serializers.py     # API序列化器
├── urls.py            # URL路由
├── services.py        # 业务逻辑服务
├── utils.py           # 工具函数
├── forms.py           # 表单定义
├── admin.py           # 管理后台
└── templates/         # 模板文件
    └── salary_management/
        ├── salary_list.html
        ├── salary_detail.html
        ├── salary_calculate.html
        └── salary_report.html
```

#### 2.2 核心服务类设计

```python
# services.py
class SalaryCalculationService:
    """工资计算服务"""
    
    def calculate_monthly_salary(self, employee_id, salary_month):
        """计算月度工资"""
        pass
    
    def calculate_income_tax(self, gross_salary):
        """计算个人所得税"""
        pass
    
    def calculate_social_insurance(self, basic_salary):
        """计算社会保险"""
        pass

class SalaryReportService:
    """工资报表服务"""
    
    def generate_department_report(self, department_id, month):
        """生成部门工资报表"""
        pass
    
    def generate_salary_slip(self, employee_id, month):
        """生成工资条"""
        pass
```

### 3. 前端设计方案

#### 3.1 UI框架选择
- **Bootstrap 5**: 响应式布局和组件
- **jQuery**: DOM操作和AJAX请求
- **Chart.js**: 数据可视化图表
- **DataTables**: 表格数据展示和操作

#### 3.2 页面结构设计

```html
<!-- 基础模板结构 -->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>人事公司工资管理系统</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="{% static 'css/custom.css' %}" rel="stylesheet">
</head>
<body>
    <!-- 导航栏 -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <!-- 导航内容 -->
    </nav>
    
    <!-- 主要内容区域 -->
    <div class="container-fluid">
        <div class="row">
            <!-- 侧边栏 -->
            <nav class="col-md-2 d-none d-md-block bg-light sidebar">
                <!-- 侧边栏菜单 -->
            </nav>
            
            <!-- 主内容区 -->
            <main class="col-md-10 ms-sm-auto px-md-4">
                {% block content %}
                {% endblock %}
            </main>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="{% static 'js/custom.js' %}"></script>
</body>
</html>
```

#### 3.3 核心页面设计

1. **工资管理首页**
   - 工资概览仪表板
   - 快速操作入口
   - 待处理事项提醒

2. **员工工资配置页面**
   - 员工列表展示
   - 工资配置表单
   - 批量操作功能

3. **月度工资计算页面**
   - 计算参数设置
   - 批量计算功能
   - 计算结果预览

4. **工资发放管理页面**
   - 发放状态管理
   - 工资条生成
   - 银行文件导出

5. **工资统计报表页面**
   - 多维度统计图表
   - 报表导出功能
   - 数据筛选功能

## 开发计划

### 第一阶段：环境准备和基础搭建（1-2天）
1. 删除旧模块代码
2. 创建新的salary_management应用
3. 设计和创建数据库模型
4. 配置基础URL路由

### 第二阶段：核心功能开发（3-5天）
1. 实现薪资等级管理
2. 实现员工薪资配置
3. 实现工资计算逻辑
4. 实现月度工资记录管理

### 第三阶段：前端界面开发（2-3天）
1. 设计和实现基础模板
2. 实现工资管理相关页面
3. 实现AJAX交互功能
4. 优化用户体验

### 第四阶段：报表和统计功能（2-3天）
1. 实现工资统计功能
2. 实现报表生成
3. 实现数据可视化
4. 实现导出功能

### 第五阶段：测试和优化（1-2天）
1. 功能测试
2. 性能优化
3. 界面优化
4. 文档完善

## 风险评估

### 技术风险
- **数据迁移风险**: 删除旧模块可能影响现有数据
- **性能风险**: 大量数据计算可能影响系统性能
- **兼容性风险**: 前端组件可能存在浏览器兼容问题

### 解决方案
- 在删除前备份重要数据
- 实现异步计算和缓存机制
- 充分测试主流浏览器兼容性

## 总结

本方案基于现有系统架构，保留优秀的基础模块，重新设计工资管理核心功能。通过合理的数据库设计、清晰的代码架构和友好的用户界面，构建一个专业的人事公司工资管理系统。整个开发周期预计10-15天，能够满足毕业设计的要求和实际业务需求。